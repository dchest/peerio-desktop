// TODO: use this component in ChatList and ZeroChats
const React = require('react');

class PlusIcon extends React.PureComponent {
    render() {
        return (
            <span className="plus-icon">+</span>
        );
    }
}

module.exports = PlusIcon;
